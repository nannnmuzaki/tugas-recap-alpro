int main() {
  printf("Alpro itu mudah!");
  return 0;
}

// kode ini menghasilkan error karena tidak terdapat '#include <stdio.h>' untuk menyertakan standar input-output library header file. Tanpa library tersebut maka compiler tidak akan mengeneali fungsi 'printf' yang menyebabkan compilation error.